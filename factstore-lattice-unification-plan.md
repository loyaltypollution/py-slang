# Superseded note

This plan belonged to the period when the framework still revolved around
`FactStore` and its overloaded lattice story.

That is no longer the right organizing interface.

The current authoritative planning note is `next-steps.md`, which describes the
post-`FactStore`, post-`DfaBlockFact` architecture and the real remaining work.
