# Superseded note

This classification note referred to the old `FactStore` / `DfaBlockFact`
architecture and is no longer authoritative.

The current framework is described in `../next-steps.md`, which reflects:

- `AnalysisStore` ownership by each `Analysis`;
- paired `.env` / `.facts` block analyses instead of `DfaBlockFact`;
- explicit citizen-split terminology;
- explicit key-space / topology vocabulary.
