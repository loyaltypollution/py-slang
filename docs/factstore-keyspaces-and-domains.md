# Superseded note

This note captured a useful intermediate audit, but it described the framework
while `FactStore` and `DfaBlockFact` were still the main explanatory surface.

The authoritative replacement is `../next-steps.md`.

Use that file for the current statements about:

- key spaces (`nodeId`, `FunctionId`, `BasicBlock`, `Unit`),
- analysis-owned storage via `AnalysisStore`,
- paired `.env` / `.facts` block analyses,
- lifecycle cleanup across context partitions,
- the current citizen split.
